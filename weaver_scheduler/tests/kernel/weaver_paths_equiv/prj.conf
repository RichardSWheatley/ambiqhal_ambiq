# Host-buildable equivalence test for the three Weaver pressure-batch
# paths (scalar / hybrid-MVE / full-MVE). Doesn't link the kernel
# scheduler module - it ships its own scalar emulations of all three
# paths and proves they produce bit-identical results on host.
CONFIG_ZTEST=y
