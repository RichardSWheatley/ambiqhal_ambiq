common:
  tags:
    - arb
    - benchmark
  harness: console
  timeout: 300
  harness_config:
    type: one_line
    regex:
      - "BENCH DONE"
    record:
      regex:
        - "RECORD:(?P<metrics>.*)"
      as_json: ["metrics"]
  platform_allow:
    - apollo510_evb
    - native_sim
    - native_sim/native/64
    - qemu_cortex_m3
  integration_platforms:
    - native_sim
tests:
  arb.bench.sched_jitter: {}
  arb.bench.sched_jitter.loaded:
    extra_configs:
      - CONFIG_ZBUS=y
      - CONFIG_LOG=y
      - CONFIG_BENCH_LOAD=y
